# International gallery

Add event photos to this folder and they will appear automatically on `/international/`.

Supported extensions: `.jpg`, `.jpeg`, `.png`, `.webp`.

Use descriptive filenames, for example `audience-main-hall.jpg` or `networking-break.webp`.
